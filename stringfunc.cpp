#include<iostream>
#include<string>
using namespace std;
    int main() {
   /* string s = "i am here";
  //  cout << s.empty() << endl;
   //  cout << s.at(20);
 //  cout << s[20] << endl;
 s.append(" in lahore ");
 cout << s;
 string at = "I have some fruits";
     s.append(at , 2 , 9);
     // appends/concatenate at string in s sting from index 2 and takes 9 characters
     cout << s << endl ;
     string arr = "Aoa ";
     arr.append(3,'!');
     arr.append(s.begin() , s.begin() + 4);
     cout << arr << endl;*/
   //  string arr = "i live in pakistan";
     // erase using .begin() or .end() 
     //arr.erase(arr.begin() );
     // erase using position and length .erase(pos,len)
    // arr.erase(2,4);
    // erase using index only to the end of string
  //  arr.erase(2);
     //cout << arr << endl;
   /*  string arr = "I am a student of PUCIT new campus";
     cout <<  arr.find("student") << endl;
      string s = "Hello World";

    size_t opp = s.find("World");

    if (opp != string ::npos){
        cout << "Found at index: " << opp;}
    else{
        cout << "Not found";}*/
    //  double ht = 345.32;
      //string s = to_string(ht);
     // cout << s.at(1);
     /* string s = "iamhere";
      int i = 1;
      s.erase(i,1);
      cout << s;*/
      cout << 2 * 3 / 4 << '\n' ;
            cout << 2 / 3 * 4 ;
return 0 ;
}
