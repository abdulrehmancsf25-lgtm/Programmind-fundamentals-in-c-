#include<iostream>
#include<cstdlib>
#include<ctime>
#include<cmath>
using namespace std ;
int main(){
  //srand(time(0));
   //int a = (rand() % 90) + 10   , b = (rand() % 90 ) + 10 , c = (rand() % 90 ) + 10  ;
   //cout << a <<" " << b << " " << c << endl;
    
    return 0 ;
}
//// Formula: (rand() % (max - min + 1)) + min
// to generate fixed size random numbers 